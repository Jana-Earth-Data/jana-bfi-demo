.next
node_modules
.env
.env.*
!.env.local.example
dist
coverage
keys-do-not-commit/

# Secrets — never commit these
*.key
tts.key*
*.pem
.aws/

# macOS / build artifacts
.DS_Store
tsconfig.tsbuildinfo

# Precomputed portfolio JSON — 20-50 MB build artifact regenerated by
# scripts/precompute-portfolio.ts during `next build`. Never commit.
lib/data/precomputed-portfolio.json
