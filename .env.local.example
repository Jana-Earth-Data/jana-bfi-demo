NEXT_PUBLIC_API_URL=https://api-test.jana.earth
NEXT_PUBLIC_AUTH_URL=https://auth-dev.jana.earth
NEXT_PUBLIC_DEMO_USE_MOCKS=false

# Supabase — Project URL plus two keys from Supabase → Settings → API
# Service role key is server-side only; never expose to the browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# One-off secret to gate the /api/admin/seed endpoint
SEED_ADMIN_TOKEN=
